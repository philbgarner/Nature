local settings = {
    relative_paths = true         -- If enabled, the editor will look for the assets/asset.list.lua file in the execution path.
    ,absolute_path = ""   -- If relative_paths = false then this is the path to search in (This should be the folder the main.lua file is in).
  }
  
return settings